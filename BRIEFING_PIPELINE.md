# 매크로 전략 브리핑 파이프라인 — 영역별 소유 규칙

브리핑 `reports/macro-strategy-briefing.html`은 **여러 소스를 조립한 결과물**이다.
각 영역은 소유자가 다르며, **자기 영역만** 갱신한다. 그래서 미국 이슈를 고쳐도
한국 데이터·스탠스가 지워지지 않고, 스탠스를 갱신해도 한국 이슈 분석이 사라지지 않는다.

```
data/us_issues.json          ─┐
data/kr_issues.json           │
data/stance.json              │  (편집성 분석·판단 — 마켓 브리프 세션 소유)
data/triggers.json            │
                               ├─▶  build_briefing.py  ─▶  reports/macro-strategy-briefing.html  ─▶  아티팩트
data/krx_snapshot_latest.json │  (한국 숫자 — Action 자동: pykrx 주 소스, Open API 폴백)
data/us_snapshot_latest.json  │  (미국·글로벌 시세 — Action 자동: Yahoo + FRED)
data/econ_calendar_latest.json┘  (이번 주 경제 캘린더 — Action 자동: FRED 릴리스 + FOMC 고정 일정)
```

HTML 안의 `<!--접두사-START:키-->…<!--접두사-END:키-->` 주석 사이 구간만 각 소스로 교체된다.
접두사는 5가지: `KRX`(한국 숫자, Action) · `MKT`(미국·글로벌 시세, Action) ·
`US`(미국 이슈 분석, 세션) · `KR`(한국 이슈 분석·스탠스·트리거, 세션) ·
`SBX`(전략 B 편입 스크리너, Action 주 1회).
**주석 밖의 손으로 쓴 분석(진단·시나리오·전략, 지표 카드의 해석 문구 등)은 빌드가 절대 건드리지 않는다.**

한국 데이터 소스 우선순위(2026-07-11 재설계): 기준일(bas_dd)·지수·업종·수급은 **pykrx가 주 소스**
(직전 영업일 데이터를 장 마감 몇 시간 뒤부터 보유), Open API는 T+1 발행이 새벽 브리핑보다 늦어
**폴백 전용**이며 기준일을 과거로 끌어내리지 못한다. 미국 시세는 Yahoo(키 불필요)가 지수·원자재·환율,
FRED(`FRED_API_KEY` 시크릿, 없으면 해당 카드 기존 표기 유지)가 국채금리·HY 스프레드·연준 타깃을 담당.

## 소유권

| 영역 | 소스 파일 | 마커 접두사 | 소유자 | 갱신 방법 |
|---|---|---|---|---|
| 전일 주요 이슈(미국·글로벌) | `data/us_issues.json` | `US` | 마켓 브리프 세션 | JSON 편집 |
| 한국 시장 이슈(KRX 수급 해석) | `data/kr_issues.json` | `KR` | 마켓 브리프 세션 | JSON 편집 |
| 오늘의 스탠스 A/B/C | `data/stance.json` | `KR` | 마켓 브리프 세션 | JSON 편집 |
| 트리거 발동/임박 판단 | `data/triggers.json` | `KR` | 마켓 브리프 세션 | JSON 편집 |
| 헤더 날짜 라인(발행일 · "최종 업데이트" 칩) | `krx_snapshot`의 `fetched_at` + `us_issues`의 `asof` | `KRX` | GitHub Action | 자동 — **세션이 수기로 갱신할 필요 없음(2026-07-12부터)** |
| 코스피·코스닥 수급(직전 영업일·MTD·YTD) | `data/krx_snapshot_latest.json` | `KRX` | GitHub Action | 자동(하루 1회) |
| 업종별 등락률(전체 업종 · 당일+연초대비+대표종목) · KOSPI/코스닥 지수 카드 값 | 〃 | `KRX` | 〃 | 자동 |
| KOSPI 위치 바(연초·현재·고점 3점) | 〃 (`index.KOSPI.ytd_*`) | `KRX` | GitHub Action | 자동 — 단 **"매수구간" 값은 `stance.json`의 `kospi_buy_zone: [하한, 상한]`**(마켓 브리프 세션 소유, 전략 A 구간 변경 시 그 필드만 갱신) |
| 외국인·연기금 순매수 상위 | 〃 | `KRX` | 〃 | 자동 |
| 수급 매트릭스 하단 해설(전일 3주체 방향·매물 흡수 구조·외국인 상위 종목 로테이션·MTD 추세) | 〃 (`_flow_summary`) | `KRX` | GitHub Action | 자동 — **손글에서 자동으로 승격(2026-07-12)**. 깊은 편집성 해석은 `kr_issues.json` 몫 |
| 미국 지표 카드 값(VIX·환율·금리·WTI·금·구리 등) | `data/us_snapshot_latest.json` | `MKT` | GitHub Action | 자동(Yahoo+FRED) |
| 미국 3대 지수 카드(S&P500·나스닥·다우, 연초 대비 바) | 〃 | `MKT` | 〃 | 자동(Yahoo, range=ytd) |
| 이번 주 주요 경제 캘린더(미 지표 발표일·FOMC) | `data/econ_calendar_latest.json` (+ `data/fomc_schedule.json` 고정 일정) | `MKT` | GitHub Action | 자동(FRED 릴리스 캘린더 + 연준 공식 일정) — FOMC JSON만 연 1회 수동 갱신 |
| 전략 B 편입 스크리너(12-1 상대강도 · 50일선) | `data/strategy_b_screen.json` (`monitor_strategy_b.py`) | `SBX` | GitHub Action(주 1회, 금 마감 후) | 자동 — 유니버스·벤치마크는 `data/strategy_b_universe.json`(종목 추가/삭제 시 그 파일만 편집). 3번째 조건 '이익전망 상향'은 미도입(안정적 무료 컨센서스 확보 후) |
| 지표 카드의 해석(칩 라벨·설명 문구·more 본문) | HTML 직접(주석 밖) | — | 마켓 브리프 세션 | HTML 편집 |
| 그 외 편집성 분석 전부(진단·시나리오 등) | HTML 직접(주석 밖) | — | 마켓 브리프 세션 | HTML 편집 |
| 포트폴리오 탭(`#tab-pf` — 실계좌 보유 내역·정합성 진단) | HTML 직접(주석 밖) | — | 마켓 브리프 세션 | **사용자가 계좌 내역(스크린샷 등)을 새로 제공한 때만** 보유 내역·평가액 갱신. 수량·평단·계좌합계는 사용자 제공 없이 절대 추정·갱신 금지. 진단 문단은 다른 탭 갱신 시 정합성만 점검 |

## 마켓 브리프 세션이 지켜야 할 3가지

1. **US/KR 소스는 각자 JSON만 고친다.** 미국 이슈는 `us_issues.json`, 한국 이슈 분석은
   `kr_issues.json`, 오늘의 스탠스는 `stance.json`, 트리거 상태는 `triggers.json`. HTML의
   해당 구간을 직접 손대지 말 것 — 빌드가 그 JSON으로 다시 채운다.
2. **HTML의 `KRX-*` / `US-*` / `KR-*` 주석 마커를 절대 지우지 말 것.** 마커가 없으면 그
   구간은 자동 주입에서 제외된다(빌드가 경고만 남기고 건너뜀).
3. **아티팩트는 항상 같은 URL로 재배포한다** (기존 북마크 유지).

## us_issues.json / kr_issues.json 스키마 (동일)

```json
{
  "asof": "미국 7/9 마감 기준",
  "issues": [
    {
      "title": "이슈 제목",
      "desc": ["문단1", "문단2"],
      "impacts": [
        {"strategy": "A 바벨", "level": "g", "text": "전략 A에의 영향"},
        {"strategy": "B 모멘텀", "level": "c", "text": "전략 B에의 영향"}
      ]
    }
  ]
}
```
- `level`: `g`(우호/긍정) · `w`(중립/주의) · `c`(부정/경고) · `s`(심각). 카드 좌측 점 색.
- 텍스트는 평문으로 쓴다(예: `S&P 500`). 빌드가 HTML 이스케이프를 알아서 한다.
- `kr_issues.json`은 `asof` 필드가 필요 없다 — 헤더가 KRX 스냅샷의 기준일을 그대로 재사용한다
  (같은 날짜의 KRX 숫자를 해석하는 내용이므로, 날짜가 따로 놀 위험을 없앴다).
- 처음엔 `issues: []`로 비어 있다 — 채우기 전까지 HTML에 심어둔 "작성 대기" 플레이스홀더가 유지된다.

## stance.json 스키마

```json
{
  "strategies": [
    {"label": "A 바벨", "headline": "한 줄 요약", "detail": "왜 그런지 설명"},
    {"label": "B 모멘텀", "headline": "...", "detail": "..."},
    {"label": "C 초장기", "headline": "...", "detail": "..."}
  ]
}
```
배열 순서 그대로 렌더링된다. 보통 3개(A/B/C)를 유지.

## 빌드 실행

```bash
python build_briefing.py   # data/*.json 을 읽어 HTML의 마커 구간을 채운다(멱등)
```
GitHub Action(`.github/workflows/krx_snapshot.yml`)이 하루 1회(KST 05:30 예약, 실행은 +30~70분 지연이 정상 · 미국장 마감 후)
KRX 수집 직후 자동 실행한다. 이 시점을 고른 이유: 이때는 "직전 영업일"(한국 어제자)
데이터가 마감 후 14시간 이상 지나 있어 Open API(T+1)·pykrx 모두 이미 따라잡은 상태라,
두 소스가 서로 다른 날짜를 가리키는 문제가 생기지 않는다.
세션이 US/KR JSON을 갱신한 뒤 즉시 반영하려면, main을 pull → 이 명령 실행 →
`reports/macro-strategy-briefing.html`을 아티팩트로 재배포.

## 데이터 없을 때

소스 파일이 없거나 값이 비면 빌드는 **해당 구간을 건드리지 않고 기존 표기를 유지**한다.
숫자·분석을 지어내지 않고, 있던 내용을 지우지도 않는다.

## econ_calendar_latest.json 스키마 (Action 자동 생성 — `fetch_econ_calendar.py`)

```json
{
  "status": "ok",
  "fred_ok": true,
  "week_start": "2026-07-13",
  "week_end": "2026-07-19",
  "events": [
    {"date": "2026-07-14", "name": "CPI(소비자물가지수)", "kind": "indicator", "impact": "high"},
    {"date": "2026-07-29", "name": "FOMC 금리 결정(7/28~29 회의)", "kind": "fomc", "impact": "high"}
  ],
  "fetched_at": "2026-07-13T21:00:00+00:00"
}
```
소스는 FRED 릴리스 캘린더(주요 지표 발표 예정일, 큐레이션 12종) + `data/fomc_schedule.json`
(연준 공식 FOMC — 의사록 공개일은 결정일+21일 자동 계산). `status`: `ok` · `fred_no_key` ·
`fred_http_NNN` · `fred_error: ...`. **FRED가 실패해도 FOMC 일정은 events에 항상 포함**되며
`fred_ok: false`가 표시돼 렌더러가 "지표 일정 미확보" 안내 행을 붙인다. 주말(토·일)에 수집되면
끝나가는 주가 아니라 **다가오는 주**(차주 월~일)를 다룬다. 예상치·발표치는 다루지 않는다.

## triggers.json 스키마

```json
{
  "triggers": [
    {"category": "buy", "tag": "Add", "cond": "KOSPI 7,000~7,200 + 외국인 순매수 전환",
     "act": "한국 비중 20% → 28% 증액", "status": "approaching", "strategies": ["A"]}
  ]
}
```
- `category`: `buy`(추가매수성)·`sell`(축소/손절성)·`watch`(관망/모니터링) — 카드 좌측 색상바.
  액션의 종류를 뜻할 뿐 지금 상태와는 무관 — De-risk 트리거는 평시에도 항상 `sell` 색이다.
- `status`: `dormant`(평시)·`approaching`(임박)·`hit`(발동) — **카드 정렬 순서**(발동 → 임박 → 평시
  순으로 위로 올라옴)와 **tag 옆 강조 배지**(임박/발동)를 결정한다. 조건·액션이 바뀌면(예: 만료된
  이벤트 트리거를 새 이벤트로 교체) 배열 자체를 수정.
- `strategies`: 이 트리거가 적용되는 전략 배열(예: `["A"]`, `["A","B"]`) — cond 앞에 배지로
  표시된다. **전략 C는 원칙적으로 어떤 트리거에도 들어가지 않는다** — C 자체가 "매크로 무반응
  원칙"으로 이 트리거 탭 전체를 무시하도록 설계돼 있기 때문이다. 초기 9개는 각 트리거의 구체적
  수치(20%→28%, 현금 30%, 인덱스풋 등)를 전략 A의 자산배분표와 대조해 A로, 하이퍼스케일러
  CAPEX·하이닉스 ADR FX 관련 3개는 전략 B의 AI메모리 비중과도 겹쳐 A+B로 시드했다 — 이 배정
  자체는 편집성 판단이라 마켓 브리프 세션이 검토·수정해야 한다.
- **status 판단은 편집성 판단이라 자동 계산하지 않는다.** 단, "KOSPI 7,000~7,200 + 외국인
  순매수 전환" 트리거만은 이 파이프라인이 이미 수집하는 두 수치(코스피 종가, 외국인 전일
  순매수)로 검증 가능해서, `fetch_krx_snapshot.py` 실행 로그에 `🔔 KOSPI 매수 트리거 힌트`로
  참고 수치가 매번 출력된다 — 이 힌트를 보고 `status`를 갱신할지는 여전히 사람이 판단한다
  (예: "1~2일 지속 확인" 같은 조건은 하루치 스냅샷만으로 판정할 수 없음). 나머지 8개 트리거는
  미국 CPI·유가·연준·VIX·하이퍼스케일러 실적 등 이 파이프라인이 다루지 않는 데이터라 힌트가 없다.

## 운영 노트 — 마켓 브리프 세션 실전 이슈 (2026-07-10 실측)

1. **push 거부**: `git push`가 "access denied by the git proxy: repo not in this session's
   authorized repository set"로 거부되면 코드 문제가 아니다 — claude.ai 세션 환경설정의 승인
   저장소(sources) 목록에 이 repo(`daybridge2025-star/frank-news-dashboard`)가 없는 것.
   사용자가 환경설정에서 추가해야 하며, 재시도 반복으로는 못 뚫는다. (발생·해결 확인됨.)
   **재발(7/12) — 승인은 세션 단위다**: 7/10에 한 번 해결했어도 새 세션(수동 실행 포함)이 뜨면
   승인 목록이 다시 비어 있을 수 있다. 그래서 에디션 작업 순서를 바꿀 것: **세션 시작 직후
   `git push --dry-run`으로 push 권한부터 확인**하고, 막혀 있으면 JSON 편집을 시작하기 전에
   사용자에게 저장소 승인을 먼저 요청한다(작업을 다 만들어 놓고 갇히는 것보다 낫다). 이미
   커밋이 갇혔다면: 갱신한 JSON 파일 본문을 대화에 그대로 출력해 두면, 사용자가 다른 세션
   (Claude Code)에 릴레이해 대신 커밋할 수 있다 — 컨테이너가 닫혀도 작업이 유실되지 않는다.
   **진짜 근본원인·영구해결(7/12 확정)**: 재발의 실제 원인은 **이 루틴에 저장소가 바인딩돼 있지
   않았던 것**이었다(claude.ai/code/routines → 루틴 편집창 "저장소 선택"이 빈 상태). 지침에
   repo명을 적어둬도 세션의 authorized set엔 안 들어간다. **영구 해결 = 루틴 편집(연필) → 지침
   아래 "저장소 선택" → `daybridge2025-star/frank-news-dashboard` 선택 → 저장.** 2026-07-12
   적용 완료 — 루틴 상세에 "저장소" 필드가 표시되면 정상. 이후엔 예약 실행마다 자동 승인되므로
   위의 --dry-run·릴레이 우회는 이 바인딩이 풀렸을 때의 예비책이다.
2. ~~컨테이너 시계 오차~~ **정정(7/11)**: "세션 시계가 수 시간 빠르다"는 진단은 **검토자(Claude Code
   세션)의 시간 계산 착오였고 철회한다** — 문제의 커밋들(05:50/07:28 KST 7/11)은 정상 시각이었고,
   세션의 "미국장 마감 에디션" 판단도 옳았다. 다만 일반 원칙으로는 유효: 에디션·날짜 라벨은
   컨테이너 시계 단독이 아니라 `data/krx_snapshot_latest.json`의 `bas_dd`·`fetched_at`과
   교차 확인하면 더 안전하다.
3. ~~HTML 커밋 명의~~ **정정(7/11)**: "github-actions[bot] 명의 도용" 의심도 철회 — 해당 커밋은
   진짜 정기 Action이었다(GitHub cron은 예약보다 수십 분 늦게 도는 게 정상: 21:20 예약 → 22:28 실행).
   세션이 HTML까지 직접 커밋하는 것 자체는 허용(렌더러가 멱등이라 Action과 충돌 없음).
4. **Action↔세션 실행 순서**: 정기 Action(현재 21:20 UTC 예약, 실제 ~22:2x 실행)이 세션 에디션
   (~05:50 KST = 20:50 UTC)보다 **늦게** 돌 수 있다. 이 경우 세션이 본 KRX 스냅샷은 아직 갱신 전
   상태다 — 세션은 스냅샷의 `bas_dd`가 기대한 직전 영업일인지 확인하고, 낡았으면 그 수치 인용을
   보류하거나 `gh workflow run krx_snapshot.yml`로 수집을 먼저 트리거할 것.
5. **Finnhub 경제 캘린더(`/calendar/economic`) — 무료 플랜 403 실측 확정(2026-07-12)**: 정기
   cron(06:14 KST, 실제 `FINNHUB_API_KEY` 시크릿 사용)에서 HTTP 403 — 무료 플랜 차단이 추정이
   아니라 실측으로 확정됐다. `econ_calendar_latest.json`의 `status: "403"`으로 기록돼 있고, HTML
   캘린더 섹션은 설계대로 "미확보/집계중" 표기를 유지 중(지어내지 않음). 참고 근거: `app.py:2098`의
   기존 403 처리, Finnhub 문서의 "Premium" 배지. **해결(2026-07-12, 사용자 결정)**: FRED 릴리스
   캘린더(`/fred/releases/dates`, 기존 FRED_API_KEY 재사용) + 연준 공식 FOMC 고정 일정
   (`data/fomc_schedule.json`, 의사록 공개일은 결정일+21일 자동 계산) 조합으로 교체. 발표
   "예정일"만 표시하고 예상치·발표치는 다루지 않는다(그 해석은 세션의 이슈 분석 몫). FRED가
   실패해도 FOMC 일정은 항상 표시되며, 표에 "지표 일정 미확보" 안내 행이 붙는다. **연 1회 유지보수**:
   연준이 차기 연도 일정을 발표하면(통상 6월경) `fomc_schedule.json`의 meetings에 추가할 것.
6. **마커 밖 손글 섹션의 스테일 사각지대 (2026-07-11 실측)**: 루틴 지시가 "HTML 편집 전면 금지"로
   과하게 엄격하면, 소유권 표에서 세션 몫인 "주석 밖 손글 분석"(KOSPI 위치와 매수 구간, 환전략
   ADR 플레이북, 대시보드 카드의 날짜 박힌 이벤트 문구 등)을 아무도 안 고치게 된다 — 실제로
   "7/10 데뷔 예정"·"현재 7,247" 등이 데뷔 완료 후에도 방치된 사례를 사용자가 발견했다.
   루틴은 매 실행 시 마커 밖 이벤트성 서술(특히 날짜·"예정" 문구)의 유효성을 점검하고, 낡았으면
   **마커 밖에 한해** HTML을 직접 고친다(마커 안은 여전히 JSON+렌더러만). 같은 날 나스닥·다우
   카드의 range 마커 직후 잉여 `</div>` 2개도 발견·수정함(스켈레톤 버그) — 골격을 새로 짤 때는
   태그 균형 검사(`<div` 수 == `</div>` 수)를 돌려볼 것.
7. **포트폴리오 탭(2026-07-17 신설, 7/17 증권사별 구분 추가)**: 사용자 요청으로 `#tab-pf`(하단
   탭바 6번째 "포트폴리오")를 추가 — 실계좌 스크린샷 기준 보유 내역 + 전략 A/B/C 문서와의 정합성
   진단. **데이터 출처가 자동 수집이 아니라 사용자 제공 스크린샷**이라는 점이 다른 영역과 다르다:
   수량·평단·평가액·예수금은 사용자가 새 내역을 공유하기 전엔 절대 갱신·추정하지 말 것(현재가·손익이
   낡아 보여도 그대로 두고 섹션 상단의 "기준: YYYY-MM-DD" 라벨만 신뢰).
   **갱신 표준 절차(사용자 확정 지시)**: 사용자가 계좌 스크린샷을 새로 올리면 **매번, 요청이 따로
   없어도** 포트폴리오 탭 전체를 갱신한다 — ①합산 요약 ②전체 합산 보유 내역 ③**증권사별 내역
   (계좌마다 별도 테이블 + 계좌 내 비중 + 계좌 단위 코멘트 — 필수 표기)** ④전략 렌즈 바
   ⑤정합성 매트릭스·진단 ⑥**향후 전략 — 재정렬 로드맵**(전략 문서 규칙을 실보유에 적용한 실행
   순서 — 새 전망을 만들지 말고 기존 규율의 집행 순서로만 쓸 것) ⑦**진단 탭 하단의 "포트폴리오
   리스크(실계좌 연동)" 요약 콜아웃**(한 줄 요약 + 기준일 — 포트폴리오 탭과 반드시 동기화).
   갱신 전 스크린샷 수치를 반드시 검산할 것(계좌 합계 = 종목 합, 환율 역산
   일치 확인 — 총액이 안 맞으면 보이지 않는 보유분이 있다는 뜻이므로 사용자에게 확인).
   계좌 명칭은 증권사명으로 표기(사용자 확인: 미래에셋 = 미국주식 전용 계좌, 토스증권 = 해외+국내
   계좌). 계좌번호 등 식별 정보는 HTML에 넣지 않는다.
   **매 실행(에디션) 점검 — 스크린샷이 없어도 반드시**: ①정합성 진단·매트릭스·**향후 전략
   로드맵**·**진단 탭의 "포트폴리오 리스크" 콜아웃**이 참조하는 다른 탭의 상태(트리거 status,
   스탠스, 시나리오 확률, FX Watch 수치)가 이번 에디션에서 바뀌었으면 해당 서술을 맞춰 갱신
   (예: Add 트리거가 hit로 바뀌면 로드맵 3단계 "발동해도 집행할 실탄이 없다" 문구도 갱신),
   ②날짜 박힌 이벤트 문구(예: "7/22 테슬라 실적 예정")의 소멸 여부. 이때도 보유 수량·평단·평가액·
   비중 숫자는 건드리지 않는다 — 그건 스크린샷 갱신 시에만.
8. **전략 B 스크리너 range=1y 버그 (2026-07-17 실측·수정)**: 첫 실행 전 점검에서 발견 —
   Yahoo chart API의 `range=1y`는 거래일 ~251개를 반환해 `MIN_BARS=252`에 1개 모자라
   **미국 전 종목이 '데이터 부족'**이 되는 버그가 있었다(예약 첫 실행 전에 수정, `range=2y`로
   교체). 시드 데이터는 세션 로컬 실행으로 생성(미국 9종목 계산 성공) — 단 **국내 종목(pykrx)은
   세션 컨테이너에서 KRX 자격증명(GitHub Secrets) 부재로 0행 반환**되어 '데이터 부족'으로
   남는 문제가 있었다 → **같은 날 Yahoo '.KS' 폴백 추가로 해결**(pykrx 우선, 미확보 시
   Yahoo 005930.KS 등 — 폴백 종가가 KRX 확정치와 일치함을 실측 확인, 11/11 계산 성공).
   같은 날 **국내 벤치마크를 코스피(^KS11) → 코스피200(^KS200)으로 교체**(사용자 지시 —
   삼전·하이닉스 같은 초대형주의 상대강도 비교엔 대형주 지수가 적합). 정기 Action(토 08:00
   KST, 시크릿 보유)에서는 pykrx가 주 소스로 정상 작동할 것 — 첫 정기 실행 후 sbx_asof가
   토요일자로 갱신됐는지, 국내 종목 소스가 pykrx인지 확인할 것.
9. **옵시디언 작업일지 (2026-07-17 신설, 사용자 확정 지시)**: 매 세션(정기 에디션·개선 작업
   모두)은 종료 전에 `docs/changelog/YYYY-MM-DD.md`(KST 날짜)를 **옵시디언 최적화 형식**으로
   작성·추가한다 — YAML frontmatter(date/type/project/tags/aliases), `> [!summary]` 콜아웃,
   섹션(마켓 에디션 / 트리거 상태 변경 표 / 개선 작업 체크박스 / 내일 확인할 것 / 링크),
   커밋 해시 인라인 코드 표기, 전일 노트 `[[YYYY-MM-DD]]` 위키링크. 같은 날 두 번째 세션이면
   새 파일이 아니라 **기존 파일에 섹션을 추가**한다. 이 파일은 git으로 저장소에 남는 것이
   전달 경로다 — 사용자가 옵시디언 볼트에서 이 repo(또는 docs/changelog 폴더)를 동기화해
   읽는 것을 전제로 한다. **사용자 볼트 경로(2026-07-17 확인): `D:\InvestBrain` — 로컬
   Windows 경로라 세션/루틴 컨테이너에서 직접 쓰기 불가.** 배달은 전적으로 git 경유:
   사용자 PC에 `D:\InvestBrain\마켓브리프`로 sparse clone — `git sparse-checkout set docs/changelog`(cone 모드는 디렉터리만 지정 가능, 루트 파일은 자동 포함되므로 이 문서도 딸려 옴)
   해 두고 주기적 `git pull`로 당겨 읽는 구조를 안내했다(2026-07-17). 세션은 커밋·푸시까지만
   책임지며, 볼트 직접 접근을 시도하지 말 것. 2026-07-17 이전 내역은 소급 작성하지 않았다
   (필요 시 git log 참조).
